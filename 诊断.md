# 微信公众号监听能力诊断

诊断日期：2026-09-08

## 结论

该项目**没有实现微信公众号开放平台/公众平台的服务端消息监听**，但已经实现了 **Windows 微信 4.x 桌面客户端聊天消息监听**。

现有功能通过读取并轮询本机微信数据库，监听指定聊天会话或全部可见会话，再从文本消息中提取日期、时间、地点、截止时间等信息并生成日程。因此，它不是微信公众号官方开发接口意义上的“公众号监听”。

如果公众号已经出现在当前微信账号的桌面端会话列表中，现有代码可能将该公众号会话纳入监听；但业务层只处理纯文本消息，常见的公众号图文、文章链接和卡片消息不会进入日程提取流程。

## 已实现的微信能力

### 1. 桌面端微信消息监听

- 项目依赖随仓库提供的 `wechatauto-replica`。
- 运行环境要求 Windows、微信 4.1.12+ 桌面客户端，并要求微信已经登录。
- 服务启动时默认启动微信监听线程；微信尚未登录或进程尚未运行时，会等待并自动重试。
- 监听器通过微信本地数据库获取新消息，不依赖微信公众号服务器回调。

相关代码：

- `server.py:18-28`：加载微信桥接模块并在服务启动时自动启动监听。
- `app/wechat/bridge.py:234-240`：启动后台监听线程。
- `app/wechat/bridge.py:434-445`：检查依赖和 `Weixin.exe`，然后创建 `WeChatDB`。
- `app/wechat/bridge.py:474-483`：创建 `Listener`，为目标会话注册消息回调并启动监听。
- `wechatauto-replica-main/wechatauto/db.py:2138`：基于本地消息数据库的轮询监听器。

### 2. 指定会话与全部会话监听

项目支持两种监听方式：

- 通过 `watch` 配置指定会话名称、备注名或微信内部用户名；默认监听“文件传输助手”。
- 通过 `watch_all` 监听本地会话列表中的全部可见会话。

`watch_all` 开启后，系统还会定期发现新会话并将其加入监听。会话来源是微信本地 `session.db` 中未隐藏的会话记录，没有看到专门排除公众号账号的过滤条件。

相关代码：

- `app/wechat/bridge.py:84-92`：默认监听配置。
- `app/wechat/bridge.py:458-480`：读取会话并建立监听器。
- `app/wechat/bridge.py:522-574`：解析指定会话或全部会话。
- `app/wechat/bridge.py:576-589`：发现并添加新会话。
- `wechatauto-replica-main/wechatauto/db.py:1481-1507`：从 `session.db` 获取可见会话。

### 3. 文本消息日程提取

监听到消息后，系统只处理微信数据库中类型为“文本”的消息。文本经过规则或 AI 分析后，符合条件的内容会写入日程或待办数据。

相关代码：

- `app/wechat/bridge.py:41-42`：将“文本”定义为唯一处理类型。
- `app/wechat/bridge.py:677-695`：实时消息回调及非文本消息过滤。
- `app/wechat/bridge.py:745-805`：解析日期、时间、地点等字段并生成日程或待办。

## 对公众号会话的实际支持范围

现有方案对公众号的支持是间接且有限的：

| 场景 | 当前状态 | 说明 |
| --- | --- | --- |
| 监听 PC 微信中的公众号纯文本消息 | 可能支持 | 前提是公众号会话存在于本地可见会话列表，并被指定监听或开启了全部会话监听。 |
| 监听公众号文章或图文卡片 | 不支持 | 微信数据库将此类内容归为“文件/链接/卡片”，业务层会跳过非文本消息。 |
| 接收用户发给自有公众号的消息 | 未实现 | 没有微信公众号服务器回调入口。 |
| 接收关注、取消关注、扫码、菜单点击事件 | 未实现 | 没有公众号事件 XML 的接收与解析逻辑。 |
| 调用公众号客服消息、模板消息等接口 | 未实现 | 没有 access token 获取和公众号 API 客户端。 |
| 公众号消息验签与加解密 | 未实现 | 没有 `signature`、`timestamp`、`nonce`、`echostr`、消息加解密等逻辑。 |

底层消息类型映射位于 `wechatauto-replica-main/wechatauto/db.py:67-75`，其中类型 49 被标记为“文件/链接/卡片”；当前业务层在 `app/wechat/bridge.py:687-695` 明确跳过所有非文本消息。

## 缺失的微信公众号官方接入组件

在项目业务代码中未发现以下公众号服务端接入要素：

- 公众号 `AppID`、`AppSecret`、服务器配置 `Token` 或消息加解密密钥配置。
- 微信服务器 URL 验证接口，包括 `signature` 和 `echostr` 校验。
- 接收公众号 XML 消息及事件通知的 HTTP 回调路由。
- 明文、兼容或安全模式下的消息签名验证与 AES 加解密。
- 文本、图片、语音、视频、位置、链接等公众号消息类型解析。
- 关注、取消关注、扫码、上报位置、菜单点击等事件处理。
- 获取和缓存公众号 `access_token` 的逻辑。
- 调用客服消息、素材、菜单、模板消息等公众号 API 的客户端。

项目目前暴露的 `/api/wechat/status`、`/api/wechat/start`、`/api/wechat/stop`、`/api/wechat/scan` 和 `/api/wechat/config` 都是本地桌面端监听的管理接口，并不是供微信公众平台调用的回调接口。

## 风险与限制

1. **运行环境受限**：依赖 Windows 和正在运行的微信桌面客户端，不能直接部署为普通 Linux 云服务。
2. **接口稳定性有限**：消息读取依赖微信本地数据库结构、数据库密钥和客户端进程，微信升级后可能需要适配。
3. **公众号内容覆盖不完整**：仅处理纯文本，公众号最常见的图文和链接内容会被忽略。
4. **不是官方服务端能力**：无法可靠接收公众号运营账号相关的用户消息和事件，也不具备官方 API 调用能力。
5. **会话数量上限**：连接时调用 `get_sessions(limit=300)`，全部会话监听实际只覆盖最近返回的最多 300 个可见会话。

## 建议方案

### 方案 A：只需读取个人微信中收到的公众号内容

可以沿用当前桌面端监听架构，并补充以下功能：

1. 识别公众号账号或 `gh_` 开头的内部用户名，并提供单独的公众号会话筛选配置。
2. 放开对“文件/链接/卡片”消息的过滤。
3. 解析消息 XML 或结构化字段，提取公众号文章标题、摘要和 URL。
4. 为文本、文章卡片和其他消息类型分别记录处理结果与失败原因。
5. 添加真实微信环境的兼容性测试，验证不同公众号消息格式。

这种方案改动相对较小，但仍然依赖 Windows 微信客户端，不等同于官方公众号接入。

### 方案 B：作为公众号运营方接收消息和事件

应新增独立的微信公众号服务端模块：

1. 增加公网 HTTPS 回调路由，并实现微信服务器 URL 验证。
2. 增加公众号凭据的安全配置与存储方式。
3. 实现签名校验、XML 解析以及安全模式消息解密。
4. 建立公众号消息和事件的统一数据模型。
5. 将符合条件的公众号消息接入现有日程提取流程。
6. 实现 `access_token` 缓存、刷新和官方 API 错误重试。
7. 增加回调幂等、超时控制、日志脱敏、安全审计和自动化测试。

这种方案才属于完整、正式的微信公众号监听实现，也更适合服务器部署和长期维护。

## 最终判定

项目当前状态应描述为：

> 已实现 Windows 微信桌面客户端的本地聊天消息监听与文本日程提取；未实现微信公众号公众平台的官方服务器回调监听。公众号会话中的纯文本消息可能被现有桌面端机制间接监听，但公众号图文卡片、官方事件回调和公众号 API 均不在当前实现范围内。
