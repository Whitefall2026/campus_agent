# 更新日志

## 2.0 - 2026-09-21

- 长期目标改为服务端 `goals.json` 持久化，重启应用后不再丢失；浏览器本地存储
  仅作为离线缓存与迁移来源。
- 服务端数据统一采用原子写入（临时文件 + fsync + 替换），读取失败时先备份损坏
  文件，避免静默返回空数据造成覆盖。
- 修复课程表、日程规划与微信读取链路中的数据丢失、并发竞态问题。
- 接口补充日期校验、本地访问保护与请求体大小限制。
- 修复页面渲染的 XSS 隐患，表单重绘时保留未保存内容。
- 测试补充长期目标持久化与非法数据拒绝等回归用例。

## 仓库整理 - 2026-09-17

- 移除误入代码仓库的路演材料（`deliverables/`、`materials/`、根目录的 PPT 与设计工程文件），以及测试输出日志 `tests/run_result.txt`。
- 校徽源文件由根目录移至 `assets/brand/ruc-logo.png`，README 界面截图移至 `docs/images/chat-preview.png`。
- `.gitignore` 增加 `*.bak.json` 等规则，防止运行时数据的备份副本被提交。
- 测试在导入应用模块前把数据目录指向临时目录，不再读写本机 `data/`。

## 1.1 - 2026-09-15

- 新增长期目标页面，支持目标添加、编辑、删除和截止时间排序。
- 自动计算剩余或逾期天数，并区分正常、7 天内到期、当天截止和已过期状态。
- 长期目标使用浏览器本地存储持久化，首次打开内置两条可删除的示例目标。
- 安装器内置随 Windows 启动选项，登录后可在系统托盘后台运行。

## 1.0.1 - 2026-09-08

- 前一日及更早仍未完成的普通日程会自动转入待办池，等待重新规划。
- 导入课表生成的课程不会自动顺延。
- 顺延时保留原日期、开始时间和结束时间等追溯信息。
- 微信后台写入与页面/API 读取采用相同的跨日处理规则。
- 补齐 Windows 桌面启动器、PyInstaller、Inno Setup 与校验文件构建流程。

## 1.0.0 - 2026-09-07

- 首个 Windows 安装版，包含本地页面、日程与待办、课程表、规划和微信读取能力。
