# 功能验收清单（ACCEPTANCE.md）

对照《基于处境的推理与规划》任务分解文档（模块 0~5），逐项给出
**验收动作 → 期望结果**。三类判据：

- **A（自动化）**：一条命令即可复验，无需人工；
- **M（手动）**：需要打开浏览器/微信人工操作；
- **D（数据证据）**：检查 `data/` 下的落盘文件。

运行前先执行：`python server.py`，浏览器打开 http://127.0.0.1:8000
（测试会用到的 `data/` 文件由系统自动创建，误操作可删除重置）。

---

## 0. 前置自动化判据（A）

```bash
python -m unittest discover -s tests      # 期望：Ran 45 tests … OK
python -m compileall app tests server.py  # 期望：无报错
node --check static/app.js                # 期望：exit 0（改过前端后）
python -m app.planner.demo                # 期望：打印预算/排程/预警表格
```

## 1. 模块 0 / 1.2：任务模型与能量规划引擎（A + D）

| # | 验收动作 | 期望结果 | 判据 |
|---|---|---|---|
| 1.1 | 新建带截止的复杂任务（见 §5 输入样例） | 落库条目带 `energy_cost`(1~5)、`deadline_type`、`duration_min`、`deliverable` | A/D |
| 1.2 | 软线任务（标题含 初稿/周报 等） | `deadline_type=soft` 且 `ddl_float_days=2`；硬线（考试/答辩等）为 `hard`、浮动 0 | A/D |
| 1.3 | `python -m app.planner.demo` | 排程不落入已有日程/课程时段；总能量 ≤ 预算；无任务可匹配时段标记「休息/机动」 | A |
| 1.4 | 造一个全天排满的日期 | 未排入任务进入「明日优先」，并出现「今日未启动，建议明早优先处理…」阻塞文案 | A/M |
| 1.5 | 同 1.4，但存在软线任务 | 软线任务出现「自动延后至…如果你不同意，可以手动拖回」顺延建议；点「同意顺延」后任务 `status=deferred`、deadline+1 | M/D |
| 1.6 | 勾选完成某项 → 返回列表点 轻松/正常/吃力 | `data/planner_events.json` 新增 `done`/`rating` 事件；`planner_profile.json` 对应小时系数按 EMA 变化 | D |

## 2. 模块 1.3：计划页展示与拖拽（M + D）

| # | 验收动作 | 期望结果 | 判据 |
|---|---|---|---|
| 2.1 | 顶部进入「🧭 计划」页 | 页面出现能量环：颜色随剩余能量绿→黄→红变化（无数值） | M |
| 2.2 | 计划页看当天 | 出现今日能量/逐小时精力曲线；「排程建议」列出 AI 认为适合安排的待办（可含未来日期） | M |
| 2.3 | 排程卡含硬线任务 / 软线任务 | 硬线卡片红边、软线卡片黄虚线；当日截止有红标 | M |
| 2.4 | 耗能略超时段能量的任务 | 卡片出现「⚡适配」标签与温和说明（≤15% 容差） | M |
| 2.5 | 完成概率 <60% 的卡片 | 概率条为红色、卡片轻微抖动、下方出现拆分/延后建议文案 | M |
| 2.6 | 点「采纳」/「一键采纳排程」 | 任务写入「🗓️ 日程」页建议日期对应时段；已采纳任务不再出现在建议列表 | M/D |
| 2.7 | 把日程卡拖到另一时段 / 把待办卡拖进某天时段 | 时间更新；`planner_events.json` 出现 `move` 事件（含 from/to） | M/D |
| 2.8 | 前一日有未完成的普通日程，次日打开应用 | 该事项自动转入待办，原排期日期/时间清空，可重新规划；已完成日程不变 | A/M |
| 2.9 | 前一日课表中有课程，次日打开应用 | 课程仍保留在原日期的课表时间轴，不进入待办 | A/M |

## 3. 模块 1.1：任务拆解（AI 可选，M）

| # | 验收动作 | 期望结果 | 判据 |
|---|---|---|---|
| 3.1 | 未启用 AI 时对复杂任务调拆解 | 返回「AI 未启用…暂无法拆解」且不报错（优雅降级） | A/M |
| 3.2 | 「👤 我的」配置 API Key 并启用后，对耗能 ≥4 任务拆解 | 返回 3~7 条子任务，每条含 name/deliverable/energy_cost/deadline | M |
| 3.3 | 点采纳拆解 | 子任务进入待办池且带 `parent_id`；无“写 200 字”类过程条目 | M/D |
| 3.4 | 简单任务调拆解 | 返回「暂不需要拆解」 | M |

## 4. 模块 2：智能挡箭牌（M）

| # | 验收动作 | 期望结果 | 判据 |
|---|---|---|---|
| 4.1 | 计划页挡箭牌粘贴「周末团建一整天」 | 三档之一（宽松/谨慎/婉拒），文案冷静无负面词 | M |
| 4.2 | 负载高（≥6 项待办或排满 95%）时再评估 | 结论为「建议婉拒」且含“线上参与/看回放/先标待定”选项 | M |
| 4.3 | 点「复制回复」 | 剪贴板获得话术 | M |
| 4.4 | 启用 AI 后再评估 | 回复为 LLM 润色（接口 `copy_method=ai`），未启用时自动用规则话术 | M |

## 5. 模块 3：风险评估（A + M + D）

| # | 验收动作 | 期望结果 | 判据 |
|---|---|---|---|
| 5.1 | 完成若干任务并打反馈后再次规划同日时段 | 完成概率受历史影响（同星期×时段） | D |
| 5.2 | 检查 `/api/plan` 响应 | 每个 entry 含 `probability/risk`，风险条目带 `risk_copy`；模拟种子确定（同日结果稳定） | A |
| 5.3 | 构造低历史完成率时段 | 对应任务概率 <60% 并触发风险 UI | A/M |

## 6. 模块 4：双轨截止（A + M）

| # | 验收动作 | 期望结果 | 判据 |
|---|---|---|---|
| 6.1 | `POST /api/plan/classify {id}` | 返回 `deadline_type`（未启用 AI 时 `method=rule`，启用后可能 `method=ai`） | A |
| 6.2 | 顺延后的软线任务第二天回到计划页 | 作为候选重新出现（`plan_defer_to` 语义生效），可手动拖回任意时段 | A/M |

## 7. 回归与数据闭环（D）

| # | 检查点 | 期望 |
|---|---|---|
| 7.1 | `data/todos.json` 样例条目 | 含 `energy_cost / deadline_type / ddl_float_days / deliverable / parent_id` 等字段 |
| 7.2 | `data/planner_events.json` | 含 `plan_apply / defer / move / done / rating` 类型事件 |
| 7.3 | `data/planner_profile.json` | 24 个逐小时系数；打过「吃力」的时段系数变小 |
| 7.4 | 删除 `data/` 后重启服务 | 全功能仍可用（默认曲线 + 空任务池，AI/微信需重配） |

## 8. 实现边界说明（判断“完全”时请先对齐这些约定）

- **AI 能力是可选项**：拆解 / 话术润色 / DDL 分类需要用户自己的 API Key
  （仓库既有 AI 惯例）。未配置时**所有功能仍可用**，走规则兜底——文档中
  “调用 LLM”的部分按“启用后增强、缺省降级”验收；
- **自动顺延是建议制**：文档的“自动将软线顺延”实现为“方案 + 一键同意”，
  避免未经确认改动用户数据（与“先预览再采纳”的产品原则一致）；
- **拖拽**用原生 HTML5 实现（无 dnd 库），移动端不支持拖拽（可用“规划到日程”弹窗替代）；
- `winsdk`（朋友圈 OCR 辅助）需要 VS C++ 工具链编译，未安装，不影响本清单
  中除“朋友圈 OCR”外的任何条目；
- 环境限制：DSH 沙箱内无法启动浏览器做 M 类检查，M 类条目需在本机浏览器完成。

## 结论口径

1. 全部 **A 类**通过（当前基线：45 tests OK、compileall/JS 检查通过）；
2. 全部 **M 类**按上表逐行复现；
3. 全部 **D 类**字段/事件可见；
4. 边界说明与协作方达成一致 ——

满足以上四条，即可判定「功能已按文档完全实现」。
